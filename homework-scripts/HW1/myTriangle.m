%% Calculate the area of a triangle given BASE and HEIGHT.
%   b and h must be 1x1 doubles for this to 
%   work as intended.
%   b : length of base of triangle
%   h : length of height of triangle

function [A] = myTriangle(b, h)
    A = (b * h) / 2;
end
